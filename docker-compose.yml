version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5555:5555"
    depends_on:
      - kvrt

  kvrt:
    build:
      context: ./backends/kvrt
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
